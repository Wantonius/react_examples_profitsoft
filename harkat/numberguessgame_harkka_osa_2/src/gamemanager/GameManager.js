import React from 'react';
import GameContext from './context/GameContext';

const GameManager = (Component) => {
		//TODO: luo decorator joka hyödyntää GameContext.Consumeria. Muista siirtää
		//myös muualta tulevat propsit eteenpäin. Käytännössä kopio esimerkistä.
}
export default GameManager;